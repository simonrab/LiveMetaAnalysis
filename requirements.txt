# Runtime dependencies for the deployed FastAPI backend (Railway / Nixpacks).
# Mirrors pyproject's runtime deps; the app runs from the repo root so the
# `livemeta` package itself does not need installing, only its dependencies.
# Pure-Python stats engine — no R/metafor in the image (see LIVEMETA_STATS_ENGINE=python).
httpx>=0.27
anthropic>=0.40
mcp>=1.2
pydantic>=2.7
numpy>=1.26
scipy>=1.11
pandas>=2.1
statsmodels>=0.14
pymare>=0.0.4
matplotlib>=3.8
fastapi>=0.110
uvicorn[standard]>=0.29
websockets>=12.0
psycopg[binary]>=3.1
