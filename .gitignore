# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ipynb_checkpoints/

# macOS
.DS_Store

# Environment / secrets
.env
.env.local

# Living-review snapshot store (Slice 2 stand-in for SQLite)
.livemeta_data/
.livemeta_local/

# Node / build artifacts
node_modules/
web/dist/
# static_web/ is the built SPA the backend serves; it must be uploaded by
# `railway up` (which honours .gitignore), so it is intentionally not ignored.

# Vercel CT.gov egress proxy (deployed standalone; kept out of the public repo)
vercel-ctgov-proxy/
